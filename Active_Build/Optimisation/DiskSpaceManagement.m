function []=DiskSpaceManagement(paramoptim,iterstruct)
   % function which handles data deletion  
    
    
   
   
   
    
end

function []=SelectDeletionMode(paramoptim)
   % selects what to keep and delete based on the current set of parameter options
   
   
   % CG
   % keep 1 on odd best on even
    
   
   %DE
   % Keep best only when it is the best ever.
end